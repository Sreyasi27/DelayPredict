# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ML
scikit-learn>=1.5.0
numpy>=2.0.0
pandas>=2.2.0
joblib>=1.4.0

# HTTP
httpx>=0.27.0

# Config
python-dotenv>=1.0.1

# Data validation
pydantic>=2.7.0

# Google Gemini AI (pinned to avoid firebase-admin conflict)
google-generativeai==0.8.3
google-api-core==2.19.2
googleapis-common-protos==1.63.2
protobuf==4.25.5
google-auth==2.29.0

# Firebase (optional — falls back to in-memory if not configured)
firebase-admin==6.5.0
